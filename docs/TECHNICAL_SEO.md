# Public HTML and crawl boundaries

The public site builds five complete HTML documents: `/`, `/journal/`, both published article paths, and `/mentions-legales/`. Their source is the same React content supplied by `apps/web/src/editorial/ssr.tsx`; the build uses Vite `ssrLoadModule` and injects the rendered markup into the built document. CSS comes from the ordinary hashed production build. Only `/` retains the JavaScript entry, which preserves the existing `/#/app` workspace. Journal and legal pages do not require JavaScript.

Each page has its own title, description, absolute canonical URL, Open Graph/Twitter metadata and the structured data supplied by the editorial module. Structured JSON escapes HTML delimiters before insertion. Article and breadcrumb facts must match visible content; the builder does not invent dates, authors, ratings or FAQ claims. Missing content or a mismatched canonical fails the build.

The primary `guteneo.com` preview allows indexing of these public pages and publishes a sitemap containing only those five canonical HTTPS URLs. Private API/auth/media routes remain blocked. The fragment workspace has no separate URL in the sitemap; fragments are not used as editorial routes. The `workers.dev` preview returns `X-Robots-Tag: noindex, nofollow` and a disallow-all robots response. The backend build also contains noindex metadata, static `_headers`, and disallow-all robots; its dynamic API noindex header is maintained by the API middleware.

Workers Static Assets uses real missing-file 404s, without the former SPA fallback. Folder index documents use trailing slash canonical routes. Release verification maps every nested `/index.html` asset to its canonical folder route, verifies its exact bytes, and never follows arbitrary redirects. The public-page generator runs before the source snapshot recheck and asset-manifest hash; both build scripts include it and the release verifier in their snapshot scope. A source change during rendering aborts the release candidate.

Tests cover generated raw HTML, safe metadata, no-JavaScript pages, incomplete-build rejection, the primary/fallback header distinction, preserved backend 403s, 404s and canonical redirects. Browser checks inspect HTTP source and navigate the journal with JavaScript disabled. Build/test/deployment results are recorded separately when run; crawl eligibility does not establish actual Google indexing or ranking.

Before moving the primary domain to `guteneo-app`, explicitly change its build to permit public indexing and make the static noindex policy host-aware. The current backend's `indexable: false` is deliberate while it remains a duplicate on `workers.dev`; moving DNS alone would retain that noindex. Google Search Console property verification and sitemap submission have not been performed.

The two article illustrations were generated specifically for Guteneo using the image-generation tool. Originals and exact prompts are retained under `assets/editorial/source/`, with dimensions and SHA-256 hashes. Responsive WebP derivatives are served at 1536×1024 and 720×480. Visible captions identify them as contemporary illustrations rather than documentary reconstructions. Historical sources remain separate, with references beside each chapter and full links at the end.

Primary references checked 17 September 2026: [Google JavaScript SEO](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics), [robots metadata and X-Robots-Tag](https://developers.google.com/search/docs/crawling-indexing/robots-meta-tag), [sitemaps](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap), [Article structured data](https://developers.google.com/search/docs/appearance/structured-data/article), [Cloudflare HTML handling](https://developers.cloudflare.com/workers/static-assets/routing/advanced/html-handling/) and [Vite SSR](https://vite.dev/guide/ssr.html).
