# Execution plan and handoff

Local candidate, 17 September: the expert assistant journey now exposes the
current connection's mandate and remaining limits, reads immutable PDFs as
bounded page images in the conversation (including before postal preparation),
and returns explicit recovery actions. The paginated review covers every page
before issuing an approval token. Human administrator activation/renewal remains
explicit, with a direct connection link and return-to-chat guidance. See
[EXPERT_CHAT_JOURNEY.md](EXPERT_CHAT_JOURNEY.md) for proof and release boundaries.

Local candidate in `fix/document-analysis-recovery`: replace the unexplained PDF
quarantine dead end with durable bounded verification recovery, shared REST/MCP
guidance and a resumable browser view. No production migration or release has
been performed. See [DOCUMENT_ANALYSIS_RECOVERY.md](DOCUMENT_ANALYSIS_RECOVERY.md)
for the new migration, limits and hosted qualification still required.

Updated 2026-09-17. The existing foundation from PR #1 was continued into a hosted v0.2 beta. Since the controlled switch at 01:11 UTC, **https://guteneo.com** serves the production-mode application with configured managed authentication and live sending disabled. A completed signup and reconnection remain to be qualified on this canonical domain. The separate fictional preview remains available at **https://guteneo-preview.nclsppr.workers.dev**. The application's alternate address is **https://guteneo-app.nclsppr.workers.dev**; its infrastructure includes EU-jurisdiction D1/R2, queues, a private qualified antivirus and a private PDF renderer. Telnyx credentials and the Guteneo Fax API application are configured. AWS sender identity and SNS notification confirmation are verified. The dedicated Pingen application is created, its credentials are installed, and OAuth plus the configured organization read succeed; no real Guteneo dispatch has been sent. Final release identities and open activation gates are recorded in [LIVE_RELEASE.md](LIVE_RELEASE.md).

## Delivered v0.2 continuation

- The published postal increment adds an authenticated postal requirements endpoint, exact-PDF preflight and private address crop, a mobile review screen, separate browser consent for a non-sending Pingen draft, and a quote tool that reuses that draft. The private renderer passed four remote synthetic cases after using the official PDF.js compatibility build; those renderer cases made no Pingen transfer. Current application release `44d1d5b` is published with 56 verified public asset hashes on each application host and both exact-source CI runs passing on their first attempt. Earlier `5b91ad3` CI failure/retry evidence remains in [TEST_RESULTS.md](TEST_RESULTS.md). The separate preview stays on `118ce087`; migrations 0001–0023 are applied remotely with exact schema/integrity checks. The consented draft flag is active, while real transfer qualification and live sending remain separate. See [POSTAL_REVIEW.md](POSTAL_REVIEW.md) and [DOCUMENT_POSTAL_PREFLIGHT.md](DOCUMENT_POSTAL_PREFLIGHT.md).
- The private Pingen qualification RPC is deployed in `5b91ad3`. On 17 September it observed a 151-centime EUR calculator result for one fictional LU page (normal paper, cheap/simplex/grayscale), created and inspected the unsent draft, deleted it and reread the durable tombstone. No customer document, tenant, dispatch, active tariff or sending permission was created. This account-specific observation is not a universal customer price or final invoice. See [PINGEN_SYNTHETIC_QUALIFICATION.md](PINGEN_SYNTHETIC_QUALIFICATION.md).
- The current `44d1d5b` release corrects postal non-delivery after handover and adds private bounded webhook setup. The four Pingen subscriptions and dedicated signature secret were installed and reread on 17 September; unsigned callbacks return 401. The current configuration version is `7eb55d92-4434-4c0d-9701-4a4e8c348f87`, with live sending disabled. A notification actually emitted and signed by Pingen remains unverified; no business records or postal sends were created. See [PINGEN_WEBHOOK_SETUP.md](PINGEN_WEBHOOK_SETUP.md) and [POSTAL_STATUS_PROJECTION.md](POSTAL_STATUS_PROJECTION.md).
- SES text-only pricing now supports the published Essentials price excluding tax, doubled at a dated commercial exchange rate frozen in the quote. This explicitly differs from a guarantee of the future AWS invoice total and needs no future invoice to qualify. No active tariff is installed; real account/sender onboarding and send qualification remain open. See [SES_TEXT_ONLY_QUOTE.md](SES_TEXT_ONLY_QUOTE.md).

- Account creation through verified Auth0 identity, with explicit signed verified-account evidence for the Auth0 Free beta and optional factual MFA, isolated organizations, profile settings, session revocation, team roles and last-administrator protection are implemented and tested. The renewed management session provisioned three dedicated Guteneo clients, its database connection, API and two Actions; browser credentials are installed and the backend reports the configured `verified_email` policy. Real signup, verification-email receipt, callback/session and reconnection still require qualification. New accounts receive a bounded PDF allowance (10 imports / 20 MiB / 3 renders per day), with a one-time EUR50 promotional balance shared across all channels, disabled top-up and channels held closed pending activation. See [AUTH0_SETUP.md](AUTH0_SETUP.md) and [WELCOME_CREDIT.md](WELCOME_CREDIT.md).
- Stripe customer linkage, invoice/payment/subscription tracking, signed webhook projection and hosted billing portal are implemented. Stripe credentials are not installed; no charge or invoice success is claimed. See [BILLING.md](BILLING.md).
- Remote MCP document listing/status, PDF import, fax preparation and explicit rescan use the same authorization/domain rules. Downloadable plugin/configuration packages cover ChatGPT, Claude, Cursor and GitHub Copilot in VS Code/CLI, with exact callback requirements and untested-host status in [LLM_SETUP.md](LLM_SETUP.md).
- Trusted quote infrastructure is implemented for fax, email and postal delivery without installing a production tariff or funding. Historical fax v2 uses immutable fixed-price snapshots; fax v3 adds a qualified range, approved firm cap and later verified-usage settlement; email freezes qualified message/attachment/FX components in nano-EUR, while postal pricing binds the exact analyzed Pingen draft and final EUR calculator response. Email/postal charges accumulate fractions across the organization before the centime ledger debit. Approval, acceptance and final provider preflight revalidate account, content, options, expiry and qualification. See [LIVE_FAX_QUOTES.md](LIVE_FAX_QUOTES.md) and [LIVE_DELIVERY_QUOTES.md](LIVE_DELIVERY_QUOTES.md).
- SES account/region limits and uncertainty handling are implemented, together with an explicit browser attestation that the email recipient requested the message. The AWS production-access request and its additional-information reply were submitted; approval has not been observed. The sandbox, verified-recipient restriction and disabled sending gates remain. See [SES_SEND_LIMITS.md](SES_SEND_LIMITS.md), [EMAIL_RECIPIENT_ATTESTATION.md](EMAIL_RECIPIENT_ATTESTATION.md) and [SES_STATUS.md](SES_STATUS.md).
- The homepage now has official assistant logos framed as perforated stamps, a cobalt dithered Luxembourg footer with automatic wing animation, dated customer price indications, and publisher/hosting legal information. Desktop and iPhone public checks pass.
- The journal contains two sourced French histories, from Gutenberg to fax/email/PDF and on printing in Luxembourg, with original blue/ivory generated illustrations. Public builds prerender six crawlable pages, including the developer guide, with canonical metadata, Article/Breadcrumb schema where applicable, sitemap and true 404s. Alternate hosts and private routes remain noindex. Search Console verification and actual indexing remain separate; see [TECHNICAL_SEO.md](TECHNICAL_SEO.md).
- `/developpeurs/` documents the browser/API/OAuth boundaries and exposes a downloadable, validated `/openapi.json`. Swagger loads only on request, with authorization and execution disabled; it never sends a document or bearer token. See [DEVELOPER_API.md](DEVELOPER_API.md).
- A localhost-only, one-use configuration form transfers credentials directly to Cloudflare secret storage without displaying values, writing a credentials file, or passing values as process arguments. The Telnyx key was installed through this path. See [SECURE_CONFIGURATION.md](SECURE_CONFIGURATION.md).
- Migrations 0001–0023 are applied to remote D1 with `quick_check=ok`, no foreign-key violations and a matching normalized schema of 177 domain objects (178 including the migration ledger). Cloudflare scanning rejects harmless EICAR, verifies exact bytes and keeps PDF processing isolated. Rescan preserves the original object/ID and uses leases, quotas and current membership checks. See [D1_MIGRATION.md](D1_MIGRATION.md), [SCANNER.md](SCANNER.md) and [SCANNER_RESCAN.md](SCANNER_RESCAN.md).

## Existing foundation

| Step                              | State and observable evidence                                                                                                                                                                                                                                    |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Inspect and qualify initial risks | Repository preserved; dated official sources, Auth0/Clerk comparison, Telnyx/SES/Pingen verification in VERIFICATION, IDENTITY_MCP and PROVIDERS. Real-client risks remain open.                                                                                 |
| Build local vertical              | Implemented on Wrangler/workerd, D1, R2 and Queues: two fictional organizations, exact import or explicit HTML rendering, preview, immutable preparation, browser approval, atomic quota/outbox, conditional attempt, per-channel simulated result and timeline. |
| Connect providers and identity    | Auth0 BFF/PKCE/JWT and official stateless MCP implemented; real clients and live provider bridge tested with signed/intercepted fixtures. These are not live account tests.                                                                                      |
| Product experience                | French printing-house landing, dither asset, onboarding, assistant connections, documents, preparation, CSV/campaigns, dispatch review, senders, usage and administration. Desktop/mobile E2E and actual PDF canvas evidence retained.                           |
| Hardening and operations          | Invariant/concurrency/content/auth/provider tests, queue/DLQ tests, local restore rehearsal, cost model, dry-runs, CI and incident runbook. See TEST_RESULTS for exact final counts and commands.                                                                |

The increment is a published application with tested local journeys and closed live-send gates. It does not complete every production requirement in the brief. No external account success or assistant compatibility is inferred from simulation.

## Decisions

- One TypeScript package with modular source folders; private R2 and D1 as the authority, separate browser service, two service classes of Queue. No VBS dependency, custom identity server, internal LLM, KV budget, microservices deployment, or automatic channel substitution.
- Approval binds the final normalized recipient, immutable document/hash, HTML/text, sender, options and monetary ceiling. The assistant can prepare/confirm after approval, but cannot create the human approval.
- Unknown provider acceptance retains its reservation and never auto-resubmits, including after lease expiry or the tested restoration. Outbox publication may duplicate safely.
- Production without a qualified scanner keeps imports in quarantine before PDF parsing. Local scan bypass is visible and forbidden on a hosted deployment.
- Do not invent live prices to unlock the product. All three production channels require immutable, qualified account-specific quotes; absent actual qualification they fail closed. Neither homepage examples nor fixture rates qualify a price. For text-only SES, the supported public price excluding tax and dated commercial FX can be qualified without a future AWS invoice. Attachment billing, fax v3 account/route qualification and verified usage-cost correlation, and Pingen account/route pricing retain their own outstanding evidence and implementation limits. See [LIVE_FAX_QUOTES.md](LIVE_FAX_QUOTES.md) and [LIVE_DELIVERY_QUOTES.md](LIVE_DELIVERY_QUOTES.md).
- Campaigns are capped at500 rows and use individual approvals. Marketing is explicitly disabled until the complete unsubscribe/reputation flow is implemented and qualified.

The actual Telnyx profile-linked rate deck and origin classes are now recorded in [TELNYX_RATE_QUALIFICATION.md](TELNYX_RATE_QUALIFICATION.md). Release `118ce087` implements and publishes the estimate, approved cap, reservation and separately verified usage settlement with migration 0023. Both exact-source CI runs and public release checks passed; no active tariff or provider cost-record correlation is inferred from this evidence. See [LIVE_FAX_QUOTES.md](LIVE_FAX_QUOTES.md).

## Next executable work, in priority order

| Work                                      | Remaining implementation or qualification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Completion evidence                                                                                                                                                         |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Trusted live quote snapshots              | Implemented for fax, email and postal: qualified immutable source/FX/account revisions, exact dispatch snapshots, atomic approval/acceptance guards and final provider preflight. Migration 0015 revokes legacy fax tariffs; migration 0016 adds email/postal quotes and cumulative nano-EUR charging. Remaining: qualify each channel’s supported tariff basis and dated FX, email attachment billing basis and any applicable tax treatment; install reviewed policy rows and qualify the fax v3 production route plus usage-cost evidence before activation. Text-only SES and fax v3 estimates do not require a future supplier invoice.                                                                                                                                                                                                | Local D1 mutation, expiry, cross-account, concurrency, fractional-credit and late-acceptance tests pass. Real tariff evidence and authorized account execution remain open. |
| Hosted infrastructure and scan            | EU D1/R2, queues, private antivirus and private PDF renderer are deployed; migrations 0001–0023 and remote integrity/schema checks are verified. The canonical domain now reaches the application. Remaining: daily antivirus definition refresh, monitoring and independent encrypted backups. Antivirus fails closed after 72 hours without fresh definitions.                                                                                                                                                                                                                                                                                                     | Retain deployed source/version proof and actual scanner/renderer evidence; qualify current definitions and a restored independent remote backup.                            |
| Real identity and assistants              | Auth0 Free, dedicated Guteneo resources, PKCE Actions, verified-account claims and browser credentials are configured, and the canonical domain reaches the deployed backend with sending disabled. Qualify real signup, received verification email, callback, EUR50 grant, logout and reconnection; restore the preview route if authentication fails. Start browser login on guteneo.com so the host-only login cookie reaches its callback. Qualify each assistant’s exact callback, consent, audience/scopes, expiry/reconnection, revocation and PDF byte transfer. Refresh tokens are not enabled in the current registrations.                               | Actual browser-session evidence and dated ChatGPT/Claude/Cursor/Copilot entries with byte hashes. Configuration and Inspector results are insufficient.                     |
| Real provider execution                   | Telnyx credentials, active LU fax number/application, T.38, callback and the application-returned outbound-profile binding are configured. SES domain and SNS handshake are verified; production-access review is pending and configuration-set sending stays disabled. Pingen OAuth, organization and exact upload origin are verified. The separate operator-only test on `5b91ad3` priced, uploaded, inspected and deleted one embedded fictional PDF draft without submission; it did not qualify the user journey. Remaining for every channel: approved tenant sender and destination policy, qualified actual tariff, supplier funds/permission, signed outcome qualification and an explicitly authorized bounded send. SES attachment billing basis and Pingen LU/international routing remain specific gates. Unknown acceptance must never trigger blind retry. | Exact authorized destinations, actual provider acknowledgement and signed/reconciled outcomes; sandbox results remain distinct from production qualification.               |
| Postal human preflight                    | The authenticated preflight/review/consented draft routes are published. A separate private probe qualified one fictional LU draft and its API deletion. Remaining: qualify real signup, exact customer-PDF review and consent, non-sending application draft, final print preview, active account/route pricing and application cleanup before any send approval.                                                                                                                                                                                                                                                                                                                                                                                                                              | Reviewable exact-PDF/address proof and approved staged send; no address overlay on an imported original.                                                                    |
| Larger campaigns and marketing            | Frozen manifest acceptance/materialization in bounded idempotent batches; complete unsubscribe/suppression/reputation policy and authorized funded quotas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Large-campaign interruption/concurrency tests, isolation and unsubscribe E2E.                                                                                               |
| Public onboarding and platform operations | Verified-email and signed verified-account onboarding on Auth0 Free (MFA optional in this beta policy), bounded IP login attempts, profile/sessions/team administration and initial PDF allowances are implemented. Remaining: stronger risk/Turnstile gating, invitations, guided sender verification, separate least-privilege platform operators, content-access consent, retention policy and alerts.                                                                                                                                                                                                                                                            | Abuse tests, role tests, actual operational qualification.                                                                                                                  |

## Reversible improvements implemented

See IMPROVEMENTS.md: final-content preflight, actionable uncertainty/channel stop, separate document budgets/resumable purge, assistant diagnostics. Native MCP Apps preview remains deferred until host authentication and file transfer are actually qualified.

Current test counts and release proof are maintained in [TEST_RESULTS.md](TEST_RESULTS.md) and [LIVE_RELEASE.md](LIVE_RELEASE.md). Historical preview evidence remains in [PUBLIC_PREVIEW.md](PUBLIC_PREVIEW.md). All 23 migrations are applied remotely; future migrations must preserve their ledger and domain invariants.

## Resume safely

Read AGENTS.md and TEST_RESULTS.md. Run `npm ci`, then `npm run demo` for local simulation; run the documented tests before further changes. Do not edit applied remote migrations, refill real quotas, clear uncertain commands, or activate live sending to make a test pass. Fixtures can set up controlled production-mode rows only inside isolated automated tests; application routes retain their gates.

No work is scheduled to continue after this session. Follow the remaining-work table and retain the existing evidence when a later milestone adds live qualification.

The earlier environment lacked Cloudflare credentials. The current desktop environment has working Wrangler OAuth authentication; no credentials were copied into the repository.
